# Benchmark Center

Displays historical benchmark data and throughput reports (e.g. TinyLlama, MoE, Diffusion). Does not execute benchmarks, but fetches existing reports via the `/v1/benchmarks` API.
